<%= simple_form_for @gift do |f| %>

  <%= f.input :name %>
  <%= f.input :price %>
  <%= f.input :category %>

  <!-- which giftlist? -->
  <!-- also need etsy listing url -->

  <%= f.submit %>

<% end %>
