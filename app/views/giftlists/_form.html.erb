<%#= simple_form_for new_user_giftlist_path(current_user.id) do |f| %>
<%= simple_form_for [current_user, @giftlist] do |f| %>

  <%= f.input :name %>
  <%= f.input :note %>
  <%= f.input :giftee, label: "Who is this list for?" %>
  <%= f.button :submit %>

<% end %>
